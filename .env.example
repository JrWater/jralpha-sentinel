# Dedicated competition paper account ONLY.
# A reused account is not eligible for judging, so these must be keys
# generated for the brand-new account, never the pre-existing one.
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_PAPER_TRADE=true

# Decision layer. The model proposes; it never holds broker credentials.
DEEPSEEK_API_KEY=
